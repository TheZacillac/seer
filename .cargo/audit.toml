# cargo-audit configuration (read by CI's rustsec/audit-check job)

[advisories]
# Both advisories are in pyo3 0.24.x code paths that seer-py never calls
# (verified: no `.nth(`/`.nth_back(`/`new_closure` anywhere in seer-py/src):
#   RUSTSEC-2026-0176 — OOB read in BoundList/TupleIterator::nth/nth_back
#   RUSTSEC-2026-0177 — missing Sync bound on PyCFunction::new_closure
# The fix is pyo3 >=0.29, but pyo3-log (our log->Python bridge) caps at
# pyo3 <0.29 as of 2026-06-12. Remove BOTH ignores when pyo3-log supports
# 0.29 — tracked in the "Upgrade pyo3 to 0.29" GitHub issue.
ignore = ["RUSTSEC-2026-0176", "RUSTSEC-2026-0177"]
